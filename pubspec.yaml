name: image_detector_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # NOTE: Camera and Image Detection
  camera: ^0.12.0+1
  image_picker: ^1.1.2
  image_cropper: ^12.2.0
  google_mlkit_object_detection: ^0.15.1
  path_provider: ^2.1.2
  path: ^1.9.0
  image: ^4.3.0
  permission_handler: ^11.3.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0




flutter:
  uses-material-design: true

  assets:
  - assets/ml_model/card_detector.tflite
